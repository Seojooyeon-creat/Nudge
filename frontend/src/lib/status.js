// The fixed default status shown when a user has no active slot selected.
// It is NOT one of the user's 7 custom slots and cannot be edited or deleted —
// "no active status" simply renders as this to friends. Selecting it in the
// picker means clearing the active status (set active to null).
export const DEFAULT_STATUS = { emoji: "❓", label: "알 수 없음" };
